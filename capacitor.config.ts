import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gh-24452-date-change',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
